<% @survey ||= survey %>
<% form_for @survey do |f| %>
  <%= f.error_messages %>
  <p><h3><%= f.label :name %>: <%= f.text_field :name %></h3></p>

    <div id="accordion">
      <% f.fields_for :questions do |builder| %>

        <%= render 'question_fields', :f => builder %>

      <% end %>
    </div>
    <p><%= link_to_add_fields "Add Question", f, :questions %></p >
  <p><%= f.submit "Save" %></p>
<% end %>
